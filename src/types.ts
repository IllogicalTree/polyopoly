export enum Group {
    brown,
    lightBlue,
    pink,
    orange,
    red,
    yellow,
    green,
    blue,
    station,
    utility
}

export enum CardType {
    chance,
    community
}

export enum BuyingStrategy {
    always,
    never,
    random
}